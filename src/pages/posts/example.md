# H1: Header One

bla bla bla

## H2: Header Two

bla bla bla

### H3: Header Three

bla bla bla

#### H4: Header Four

bla bla bla

##### H5: Header Five

bla bla bla

###### H6: Header Six

bla bla bla

This is a paragraph (`p`) containing **bold text** (`strong`), _italic text_ (`em`), and ~~strikethrough text~~ (`del`). You can also include [hyperlinks](https://example.com) (`a`) and `inline code` (`code`) snippets.

---

> This is a blockquote (`blockquote`). It is used to highlight quotes or specific callouts.

### Lists and Tasks

- Unordered List (`ul`)
- Item A (`li`)
- Item B

1. Ordered List (`ol`)
2. Step One (`li`)
3. Step Two

- [x] Completed task (`input`)
- [ ] Incomplete task

### Code Blocks

To style code blocks, target the `pre` and `code` tags.

### Tables (`table`)

| Header 1 (`th`) | Header 2 (`th`) |
| :-------------- | :-------------- |
| Cell 1 (`td`)   | Cell 2 (`td`)   |
| Cell 3          | Cell 4          |

### Media

An image (`img`) with alt text:
![Placeholder Image](https://via.placeholder.com)
